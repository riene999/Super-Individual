# Technical Overview

Super Individual：把 PM 自然语言需求转成目标仓库可提测 PR 的编排系统。本文覆盖三部分——关键工程难点与解决方案、核心技术栈、项目亮点，外加选填加分项。

---

## 1. 关键工程难点与解决方案

### 1.1 上下文召回精度（喂给 LLM 的"少而准"）
**难点**：长流水线里，喂给规划/生成的上下文一旦"多而杂"，LLM 就被噪声带偏、还烧 token；但喂太少又会臆造仓库里不存在的接口。
**解决**：分两条线提精度。
- **代码上下文**：`code_spec` 先为目标仓库每个文件建摘要 + 接口签名索引（`workspace/repo-specs/<nwo>/current.json`），再由 LLM **从全量索引里挑出与本次需求相关的文件**，并附上这些文件的真实导出符号/调用签名作为"地图"。生成新文件的 agent 还能用只读搜索工具现查现用，避免照记忆瞎写。
- **历史上下文**：记忆不是 RAG 切片，而是**结构化召回**——每次 run 沉淀 `{entities, clarifications}`，新需求按加权维度（操作类型 / 领域对象 jaccard / UI 面 / 影响层）打分，配 `min_score` 阈值 + 仓库范围过滤，只召回真正相似的旧方案。

### 1.2 断点重放（事件溯源式持久化）
**难点**：一次 run 要调多次 LLM、耗时分钟级，若中途某步走偏，全流程重跑既慢又贵。
**解决**：每次 run 的所有阶段事件**追加写成 `events/<runId>.jsonl`**（事件溯源）。重放时 `truncate_after(runId, index)` 砍掉该点之后的事件，用（可被 PM 修改的）新输入从该阶段往下重跑（`replay_from`），上游已完成的结果不必重算。前端 `ReplayBar` 让任一历史 run 从指定阶段改写重放。

### 1.3 Skill 注册热加载（改了即时生效，不重启）
**难点**：skill 方法论文档为性能在进程内做了缓存，直接改文件不会被正在跑的服务感知。
**解决**：`library_loader` 的增删改（`create/update/delete_skill`）在写盘后统一调 `reset_skill_docs_cache()` 清缓存，下一次 run 进规划阶段 `load_skill_docs()` 时重新读盘——**通过设置面板改 skill，即时对后续 run 生效，无需重启 server**。（仅"绕过面板直接编辑磁盘文件"才需重启。）

### 1.4 跨栈类型一致性（现状与权衡，诚实说明）
**难点**：同一份数据（run 请求体、skill 对象）在 Python 后端与 TS 前端各写一遍，中间是无类型的 JSON，字段漂移没有编译器兜底。
**现状**：**未做自动代码生成**。当前靠两点控制风险——后端 Pydantic 模型 + 前端 TS interface 各自在本侧强类型化；API 面刻意保持小（runs / repos / skills / metrics 几个端点），改动时人工对齐两端。要根治需引入"一处 schema → 两端生成"（如 OpenAPI→TS），属于已识别但**尚未投入**的演进项——此处不夸大为已解决。

### 1.5 验证环节的真实可靠（附带难点）
**难点**：验证若"假绿"比不验证更危险。Windows 上 `subprocess` 默认按 gbk 解码 UTF-8 输出会崩、ANSI 色码污染日志、依赖缺失会被误判通过。
**解决**：所有子进程捕获统一 `encoding="utf-8", errors="replace"` + `NO_COLOR`/去 ANSI；验证按"语法检查 → 后端加载冒烟 → 前端构建 → 测试"分级跑，失败信息回喂 LLM 针对性修复，最多 3 次。

---

## 2. 核心技术栈

| 维度 | 选型 | 说明 |
|---|---|---|
| **前端** | React 18 + Vite 5 + TypeScript | 纯 hooks（`useRun`/`useRepo`/`useSkills`），无额外状态库；用浏览器原生 `EventSource` 订阅 SSE 实时事件流。 |
| **后端** | Python 3.10+ · FastAPI · uvicorn[standard] | HTTP 端点 + SSE（`StreamingResponse`，`text/event-stream`）；编排核心是独立 Python 包 `super-individual-orchestrator`，API 以 editable 方式依赖它。 |
| **模型层** | 豆包 / Volcengine Ark（OpenAI 兼容） | 经 `openai` SDK 的 `AsyncOpenAI` 调用；`LLMClient.chat()` 统一入口，逐次记录 tokens / 延迟 / 成本。 |
| **数据库** | 无独立 DB，文件型持久化 | 事件日志 `events/*.jsonl`（事件溯源）、需求记忆 `memory/`、代码规范索引 `workspace/repo-specs/*.json`。（注：被改造的目标仓库自带 Sequelize/SQLite，那是"作业对象"的库，不是本系统的库。） |
| **中间件** | 进程内异步事件总线 + CORS | 自实现的 asyncio 发布/订阅总线（`bus.subscribe`）把流水线事件推给 SSE；FastAPI CORS 中间件。**无 Redis / 消息队列**。 |
| **集成层** | git + GitHub CLI `gh` | `conduit.py` 用 git 做 clone/commit/branch/push，用 `gh` 做 fork 与开 PR。 |
| **工程工具** | pnpm workspace（Node）· uv（Python）· ruff | 前端 5173 / 后端 3001，Vite dev 把 `/api` 代理到后端。 |
| **部署环境** | 本地运行（已在 Windows/win32 实测） | 前端 `vite build` 出静态产物、后端 `uvicorn` 进程；**当前未容器化**（无 Dockerfile），但前后端均可平凡迁移到容器/静态托管 + 进程托管。 |
| **云资源** | 豆包/Ark LLM API + GitHub | 唯二外部依赖：LLM 推理接入点、GitHub（fork/PR）。**无自建服务器、对象存储或托管数据库**。 |

---

## 3. 项目亮点（≤3，对应加分锚点）

### ① 抽象到位 —— 新模式只加 1 个 Skill 文件，不改主干（命中锚点①）
Skill 是一段**做事的方法论文档**（`skills/library/*.md`），规划阶段整段注入提示词。新增一种需求模式，**只需新增一个 `.md`**：主控流水线、各 agent、契约结构、API 全部零改动。而且这件事现在能**在网页设置面板里完成**（增删改 skill），写盘即清缓存、下一次 run 即时采纳——现场出一道新模式题，加一个文件就能支持。

### ② 断点重放 —— 事件溯源，任一阶段可暂停/改写/重放下游（命中锚点②）
全流程阶段事件按 run 持久化为 `events/<runId>.jsonl`。任意历史 run 都能从指定阶段截断、用修改后的输入重放下游，已完成的上游不重算。配合澄清环节的"暂停等 PM 回答"，整条链路是**可溯源、可干预、可重入**的，而不是一次性黑盒。

### ③ 业务上下文反哺 —— 结构化召回，相似新需求自动复用旧方案（命中锚点⑤）
每次 run 把需求结构化沉淀成记忆（实体 + 澄清问答）。新需求进来时按加权维度打分召回最相似的历史方案，作为规划参考并可预填澄清答案；不相似就不召回（阈值 + 仓库范围过滤）。这是**结构化召回**而非朴素 RAG 切片，跨 run 的"上次怎么做的"被显式复用，且 PM 可一键忽略某条历史。

> 另外可观测性（锚点④：每次 LLM 调用记 tokens/延迟/成本 + 指标面板）也已具备，作为支撑性能力；跨栈一致性（锚点③）当前为人工对齐、未做自动驱动，已在"难点 1.4"如实说明，不列为亮点。

---

## 4. 选填加分项

> 标注原则：已实现的写实，未实现的如实写"现状/方案"，不编造数据。

### 4.1 Prompt 策略 / Agent 流程图（已实现）
- **规划（plan_loop）**：自评式 ReAct —— 先让模型判断信息是否足够，**克制提问**（只问会改变方案的关键歧义，最多 3 轮，到顶强制出方案），并强制产出 **contract** 把跨文件的组件名/导出名/import 路径/函数完整签名钉死。
- **生成（code）**：新建文件走**只读工具循环**（grep / read_file / list_files），"用前先读"——用任何项目内部符号前必须先查证真实导出名与签名，查清后直接输出文件原文；验证报错会回喂做**定向修复**而非重写。
- **代码规范（code_spec）**：逐文件摘要 prompt 产出 `summary/tags/symbols/interfaces(入参→返回)`；选片 prompt 让模型从全量索引挑出相关文件。
- **通用约束**：低温度（0.1）、只输出纯文件或严格 JSON、不加解释、不引入未声明依赖。
- **Agent 流程图**：见 [`ARCHITECTURE.md`](./ARCHITECTURE.md)（mermaid 总览图 + 一次 run 的调用时序图）。

### 4.2 评测方案与样例结果
- **现状（诚实）**：无大规模自动评测集；现存确定性测试为 `e2e/test-recall-prefill.ts`（召回预填三态语义，不调 LLM）。
- **评测方案（待建）**：固定一组需求样例（加字段 / 加筛选 / 加页面 + 组合 + 边界），按 **「PR 能否生成 → verify 是否一次过 → 人工 review 通过率 → 改对文件数 / 漏改数」** 打分；并做 baseline（关召回、关 skill）vs 开启的对照。
- **样例结果（定性）**：开发期最佳一次 run 后端完全正确、前端 hook 接入与函数签名正确，典型残留是个别 service 调用漏传 auth header。**无统计性结论，仅作定性参考。**

### 4.3 性能指标（已实现，数据来自 35 个开发期 run）
> 每次 LLM 调用经 `LLMClient.chat()` 落 `llm.call` 事件，`metrics/aggregator` 聚合，前端有指标面板（当前 run 卡片 + 全局 tab）。

| 指标 | 实测值 |
|---|---|
| 累计 LLM 调用次数 | 1,447 |
| 累计 tokens | 2,225,663 |
| 累计成本（豆包） | ¥0.075（均摊每 run ≈ ¥0.0021） |
| 单次调用延迟 | 中位 8.2s · p95 21.1s · max 157.8s |

- **调用成功率 / 召回命中率（诚实）**：尚**未单独埋点聚合成面板指标**。成功率可由 `verify` 通过/重试次数事件近似观察，召回命中率可由 `recall.matched` 的 score 分布近似——原始事件已具备，做成聚合指标是明确的演进项。

### 4.4 用户反馈
- **诚实说明**：个人 / 演示阶段项目，**无外部用户研究、问卷或 NPS 数据**。产品内的"反馈回路"体现在交互上：PM 在澄清框作答、对召回的历史一键忽略（`recall.dismissed`）、对不满意的结果用断点重放改写重跑——这些都即时影响下游产物。

### 4.5 版本迭代记录（据 git 提交）
| 日期 | 里程碑 |
|---|---|
| 06-07 | 初始化 → skill 雏形 → **TS 全量重写为 Python** → 增加 停止 / 断点重放 / run 历史 / 自动开 PR |
| 06-08 | 引入 **code-spec** 代码规范索引（逐文件摘要 + 选相关文件） |
| 06-09 | **skill 重构**（关键词匹配路由 → plan_loop 规划与澄清合一）；生成端加只读搜索工具；清理死代码（旧 TS 测试 + 旧 skill 匹配机制）；新增设置面板（skill 增删改）与召回 / skill 开关 |
